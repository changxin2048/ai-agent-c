{
  "name": "aiagent",
  "version": "1.0.0",
  "scripts": {
    "install": "cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "aiagent": "file:"
  }
}
